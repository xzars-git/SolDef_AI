# Demo App Requirements
# Install: pip install -r requirements.txt

# Core ML
tensorflow==2.10.0
numpy>=1.21.0

# Image processing
pillow>=9.0.0
opencv-python>=4.6.0

# GUI (Tkinter is built-in with Python, no need to install)
